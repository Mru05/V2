export interface UploadDocument {
    id: number; // Add any additional fields as needed
    type: string;
    registrationNumber: number;
    filePath: string; // Adjust according to your backend response
}
