export interface GetDocument {
    documentId: number; // Change to documentId to match the response
    documentName: string; // Change to documentName to match the response
    documentUrl: string; // Change to documentUrl to match the response
}
